Do you want me to also lock these dependencies into a requirements.txt (with exact versions for macOS Intel) so that every install is reproducible? That way, no surprise upgrades will break your code later.